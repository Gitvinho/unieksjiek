<div class="container">
  <div class="row">
    <div class="col-12">
      {% include 'breadcrumbs' %}
      <div class="title-box">
          <h1>{{ page.title }}</h1>
      </div>
    </div>
  </div>
  <div class="row">
  {% for collection in linklists[page.title].links  %}
    <div class="col-md-4">
      {{ link.title | link_to: link.url }}
    </div>
  {%  endfor %}
  </div>
</div>