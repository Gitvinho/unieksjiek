<div class="container">
  <div class="row">
    <div class="col-12">
      {% include 'breadcrumbs' %}
      <div class="title-box">
          <h1>{{ page.title }}</h1>
      </div>
    </div>
    <div class="col-12">

      {% for collection in linklists[page.title].links  %}
        {{ collection.title }}
      {%  endfor %}
    </div>
  </div>
</div>